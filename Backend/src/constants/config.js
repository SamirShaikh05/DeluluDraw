const DEFAULT_SETTINGS = {
  maxPlayers: 12,
  rounds: 3,
  drawTime: 80,
  wordOptionsCount: 3,
  hints: 2,
};

const CHOOSE_TIME_MS = 15000;
const ROUND_TRANSITION_MS = 4500;

module.exports = {
  CHOOSE_TIME_MS,
  DEFAULT_SETTINGS,
  ROUND_TRANSITION_MS,
};
